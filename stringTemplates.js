let user = "Chandler";

let greet = "Welcome " + user + " to worldddd";
//instead of using + use backticks and dollar
//sign to access the variable
let g = `Welcome, 'heeelloo' "worlslslsls" ${user} to worlldd`;

console.log(greet);
console.log(g);
